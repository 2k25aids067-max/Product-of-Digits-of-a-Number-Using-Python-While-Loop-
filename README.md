# Product-of-Digits-of-a-Number-Using-Python-While-Loop-
This Python program calculates the product of all digits in a number entered by the user using a while loop. The program repeatedly extracts the last digit using the modulus operator (%), multiplies it with the current product, and removes the last digit using integer division (//). The loop continues until all digits are processed,.
